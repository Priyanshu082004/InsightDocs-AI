 const DOCUMENT_STATUS = Object.freeze({
  UPLOADING: "UPLOADING",
  PROCESSING: "PROCESSING",
  READY: "READY",
  FAILED: "FAILED",
});

 const DOCUMENT_STATUS_VALUES = Object.values(DOCUMENT_STATUS);

 const PROCESSING_STAGE = Object.freeze({
  OCR: "OCR",
  TEXT_EXTRACTION: "TEXT_EXTRACTION",
  CHUNKING: "CHUNKING",
  EMBEDDING: "EMBEDDING",
  SUMMARY: "SUMMARY",
  KEYWORDS: "KEYWORDS",
  RISK_ANALYSIS: "RISK_ANALYSIS",
});


 const PROCESSING_STAGE_ORDER = Object.values(PROCESSING_STAGE);

 const PROCESSING_STATUS = Object.freeze({
  PENDING: "PENDING",
  IN_PROGRESS: "IN_PROGRESS",
  COMPLETED: "COMPLETED",
  FAILED: "FAILED",
});

 export { DOCUMENT_STATUS, DOCUMENT_STATUS_VALUES, PROCESSING_STAGE, PROCESSING_STAGE_ORDER, PROCESSING_STATUS };